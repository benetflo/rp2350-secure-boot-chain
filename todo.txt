HIGH PRIO
* HTTPS ON OTA SERVER
* firmware key not hardcoded?
* CHECK PROBLEMS WITH OTP 

MEDIUM PRIO
* Helhetslosning for hur man gor OTP sakert. Tex resterande OTP slots, user content, key slots etc

TESTS
Shut off power during OTA update  
→ Testa med signerad + osignerad

Flash unsigned firmware and bootloader  
→ Testa med osignerad

Add unsigned firmware to OTA server and try updating – should fall back to working partition  
→ Testa med osignerad

Flash firmware version that is lower than minimum in OTP  
→ Testa med signerad

Try flashing firmware that is larger than SLOT SIZE  
→ Testa med signerad + osignerad

Manipulate metadata and try flashing – bootloader should not allow  
→ Testa med signerad

Tampered signed firmware (signerad men modifierad payload utan uppdaterad signatur)  
→ visar att signaturen verkligen skyddar innehållet, inte bara finns där.


TESTS MADE:


Physical tests

Flash firmware and bootloader from empty FLASH
- Flash unsigned bootloader --> nothing should happen. OK
- Flash unsigned firmware --> Yellow light followed by RED nothing more happens. OK


OTA related attacks
Signed bootloader and signed firmware A is flashed.

- Get unsigned firmware B from OTA server --> Yellow LED followed by RED led and then fall back to A program OK
- Get signed firmware B from OTA server with a lower version than minimum version in OTP --> Yellow, Red fall back to A program
- Get signed firmware B from OTA server with manipulated metadata
- Get signed firmware B from OTA server that is larger than slot size
- Power loss during OTA --> Bootloader will detect corrupt image at B and fallback to A
- Get signed firmware B with tampered payload - signatur should change and not be flashed
- Signed firmware B with wrong partition in headern -- bootloader should not allow


NOTE OTP READ WORKS FINE SOMETHING WITH OTA IS WRONG when comparing vesrsions

problem with header in OTA update so last version is still in header??

works when flashing maybe OTA has to write header also and header is not included in BIN or something?