HIGH PRIO
* HTTPS ON OTA SERVER
* RECOVERY IF POWER IS LOST DURING OTA UPDATE.
* hash metadata struct to prevent tampering with partitions.
* firmware key not hardcoded? 
* Put flash layout adresses in a flash_layout.h and write in it that it is not really secret but it can make reverse engineering alot easier if an attacker would get a hold of the layout.

MEDIUM PRIO
* Rollback protection for bootloader in OTP, use version in config to name binaries and check against OTP value
* Helhetslosning for hur man gor OTP sakert. Tex resterande OTP slots, user content, key slots etc
* clang tidy for MISRA C and other stuff.

LOW PRIO
* Fuzz testing? onefuzz?
* CLEANUP TOOLS FOLDER, MAKE CLASS AND ONE FILE?
* Write bootloader in Rust for comparison, discuss in rapport?
